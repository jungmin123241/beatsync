# 비트싱크 서비스 기획서 (PRD)

- 작성자: 한정민
- 작성일: 2026-08-03

---

## 1) 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

러닝이나 헬스 등 운동을 즐기며 스마트워치를 사용하는 사람들은, 운동 중 플레이리스트에서 지금 하는 운동 강도와 맞지 않는 음악이 나오는 불편을 겪고 있다. 비트싱크는 사용자의 실시간 심박수에 맞는 템포의 음악을 자동으로 추천해 이 문제를 해결하고자 한다.

---

## 2) 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재 사용자는 운동 전 미리 만들어둔 고정 플레이리스트를 재생하거나, 운동 중 수동으로 곡을 넘기며 자신의 컨디션에 맞는 음악을 찾고 있다. 이 방식은 워밍업·본운동·쿨다운처럼 운동 강도가 바뀌는 상황에 따라가지 못하고, 운동 중 스마트폰을 조작해야 하는 번거로움이 있으며, 심박수 같은 신체 상태를 음악 선곡에 전혀 반영하지 못한다는 한계가 있다.

---

## 3) 목표와 기대효과 (성공 기준)

- 계정 연동, 심박수 기반 곡 추천·저장, 곡 변경 안내 세 기능이 모두 동작한다
- Spotify 로그인 → 나이 입력 → 운동 종류 입력·플레이리스트 지정 → 가상 심박수로 곡 추천 → 저장 → 곡 변경 안내까지, 4번 이용 흐름이 끊김 없이 이어진다
- 추천 곡이 현재 심박수와 유사한 BPM으로 선택된다
- "저장" 버튼으로 Spotify 플레이리스트에 곡이 정상 추가된다
- Apple Music 등 다른 음악 서비스는 구현하지 않는다
- Vercel에 배포되어 실제 URL로 접속·시연할 수 있다

---

## 4) 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

**사용자:** 러닝이나 헬스 등 운동을 즐기며 스마트워치를 사용하는 사람

**이용 흐름:**
Spotify 계정으로 가입·로그인 → (최초 1회) 나이 입력 온보딩 → 운동 종류 이름 입력 및 해당 운동에 쓸 Spotify 플레이리스트 지정 → 가상 심박수 시뮬레이션 시작 → 현재 심박수와 BPM이 가장 가까운 곡 추천 → 마음에 드는 곡은 "저장" 버튼으로 지정 플레이리스트에 저장 → 심박수가 바뀌어 추천 곡이 달라지면 안내 메시지 확인 → 운동 종료

---

## 5) 주요 기능 (must / nice로 구분)

### [must] 0. 계정 연동 (공통 전제)
- 설명: 사용자는 Spotify 계정으로 앱에 로그인합니다. 심박수는 실제 기기 연동 없이 가상(mock) 데이터로 시뮬레이션합니다.
- AI가 꼭 지켜야 할 규칙:
  - 별도의 자체 회원가입은 만들지 않고, Spotify 계정 연동으로만 로그인한다 (Apple Music은 지원하지 않는다)
  - 심박수는 사용자의 최대심박수(220-나이)의 약 40%~90% 범위에서 5초마다 값이 바뀌는 가상 시뮬레이션으로 만들고, 화면에 실시간으로 표시한다
  - 앱에서 다루는 플레이리스트는 연결된 Spotify 계정의 실제 플레이리스트이며, 앱 내부에 별도 플레이리스트를 만들지 않는다
  - 나이는 최초 로그인 직후 온보딩 화면에서 한 번만 입력받아 localStorage에 저장하고, 이후에는 재입력 없이 저장된 값을 재사용한다
  - 온보딩 다음 단계에서 운동 이름을 직접 입력받고, 그 운동에 쓸 Spotify 플레이리스트를 선택하게 한다

### [must] 1. 심박수 기반 음악 추천 및 저장
- 설명: 현재 심박수와 BPM이 가장 가까운 곡 하나를 추천하고, "저장" 버튼을 눌러야 지정한 플레이리스트에 저장됩니다.
- AI가 꼭 지켜야 할 규칙:
  - 추천 후보곡은 프로젝트에 직접 준비한 샘플 곡 목록(제목·아티스트·BPM·Spotify 트랙 ID)에서 고른다
  - 샘플 곡 목록은 실제 Spotify에 존재하는 곡의 트랙 ID까지 포함해 구성해, 검색 없이 바로 실제 플레이리스트에 저장할 수 있게 한다
  - 현재 심박수와 BPM이 가장 가까운 곡 1개를 추천한다
  - 곡 저장은 사용자가 "저장" 버튼을 눌렀을 때만 실행되며, 현재 진행 중인 운동에 지정된 플레이리스트에 추가된다
  - 운동 종류는 미리 정해진 목록이 아니라 사용자가 이름을 직접 입력해 만든다

### [must] 2. 곡 변경 안내
- 설명: 심박수가 바뀌어 추천 곡이 달라지면, 화면에 곡이 바뀌었다는 안내 메시지를 표시합니다.
- AI가 꼭 지켜야 할 규칙:
  - 심박수 변화로 추천 곡이 이전과 달라지면 즉시 안내 메시지를 화면에 표시한다
  - 앱은 음악 재생을 직접 제어하지 않는다 (추천과 안내만 하고, 실제 재생은 Spotify 앱/웹플레이어에서 사용자가 직접 한다)

---

## 6) 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

**범위 (만들 것)**
- Spotify 계정 연동 로그인
- 나이 입력, 운동 종류 입력, 운동별 Spotify 플레이리스트 선택
- 가상 심박수(mock) 발생기 — 최대심박수의 약 40~90% 범위에서 5초마다 값 변경, 실시간 표시
- 샘플 곡 목록(제목·아티스트·BPM·Spotify 트랙 ID, 25곡 내외) — BPM이 겹치지 않고 고르게 분포하도록 구성
- 심박수와 가장 가까운 BPM 곡 추천 및 플레이리스트 저장
- 곡 변경 시 안내 메시지 표시

**비범위 (만들지 않을 것)**
- 소셜 기능 (친구 추가, 다른 사용자와 운동/플레이리스트 공유)
- 자체 음악 스트리밍/재생 기능 (Spotify 연동만 사용, 자체 음원 재생 X)
- Apple Music 연동 — 이번 실습에서는 Spotify만 지원
- 운동 기록 분석·통계 대시보드 (칼로리, 운동 이력 그래프 등)
- 스마트워치 자체 앱/워치페이스 개발, 실제 스마트워치 연동 (가상 심박수로 대체)
- 외부 BPM 데이터베이스 API 연동 (GetSongBPM 등) — 직접 준비한 샘플 곡 목록으로 대체
- 심박수 강도 구간(저/중/고강도) 분류, 구간별 별도 플레이리스트 전환 안내 — 최근접 BPM 추천 + 곡 변경 안내로 단순화
- 추천 시 ±bpm 범위 단계적 확대, 추천 갱신 지속시간(N초) 조건 — 최근접 1곡 추천으로 단순화

---

## 7) 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: 비공개 (내부 개발용 문서, 외부 공개 금지)
- 개인정보 여부: 심박수, 나이, 운동별 플레이리스트 지정 정보는 서버로 전송·저장하지 않고 사용자 브라우저의 localStorage에만 저장하며, 제3자에게 제공하지 않는다. (단, 브라우저를 바꾸거나 데이터를 삭제하면 저장된 정보가 사라질 수 있음을 사용자에게 안내한다)
- API 키 관리: 음악 서비스(Spotify) API 키는 `.env` 파일에 저장하고 `.gitignore`에 등록해 GitHub 등 외부에 노출되지 않도록 관리한다.

---

## 8) 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- Next.js — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- 고정 이유: Day 1 실습에서 쓴 것과 같은 구성이고, 3일차에 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
- Auth.js (next-auth) — Spotify 로그인·인증 토큰 관리 (서버 세션에 보관)
- Tailwind CSS — 미니멀·블랙&화이트 디자인을 빠르게 구현
- 데이터 저장: 별도 DB 없이 브라우저 localStorage 사용 (나이, 운동별 플레이리스트 지정, 저장한 곡 기록 등)

---

## 9) 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눈 목록 (만들 순서대로, PLAN.md와 동일)*

1. Next.js 프로젝트 초기 세팅 및 기본 화면 뼈대 만들기
2. Spotify 계정 연동 로그인 화면 만들기
3. 음악 서비스 인증 토큰 발급·저장 처리 (API 키는 `.env`로 관리)
4. 로그인한 사용자의 플레이리스트 목록 불러오기
5. 최초 로그인 직후 나이 입력 온보딩 화면 만들고 최대심박수(220 - 나이) 계산 로직 구현 (localStorage에 저장, 최초 1회만), 운동 종류 입력, 운동별 Spotify 플레이리스트 선택
6. 가상 심박수(mock) 발생기 만들기 — 최대심박수(220-나이)의 약 40~90% 범위로 5초마다 값을 변경하고 화면에 실시간으로 표시
7. 샘플 곡 목록(제목·아티스트·BPM·Spotify 트랙 ID) 데이터 파일 만들기 — 실제 Spotify에 존재하는 곡으로 25곡 내외, 트랙 ID까지 포함. BPM은 서로 겹치지 않게 하고, 값 사이가 크게 벌어진 구간이 없도록 고르게 분포시킨다
8. 가장 가까운 BPM 한 곡 선택 - 추천곡 표시, Spotify 플레이리스트 저장 버튼 구현
9. 심박수가 바뀌면 즉시 추천 - 곡 변경 시 안내 메시지 표시
10. Vercel 배포 및 환경변수 설정
