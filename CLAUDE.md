# CLAUDE.md

@AGENTS.md

이 파일은 이 저장소에서 코드를 작성하는 Claude Code를 위한 규칙서입니다. 자세한 기획 배경은 [PRD.md](PRD.md), 작업 순서는 [PLAN.md](PLAN.md), 화면·데이터·기술 설계는 [DESIGN.md](DESIGN.md), 초기 아이디어는 [prd_lite.md](prd_lite.md)를 참고하세요.

## 프로젝트 개요

**비트싱크(BeatSync)** — Spotify와 연동해 사용자의 (가상) 실시간 심박수를 측정하고, 그 심박수와 BPM이 가장 가까운 음악을 추천해주는 서비스. 러닝·헬스 등 운동을 즐기며 스마트워치를 쓰는 사람이 대상.

현재 단계: PLAN.md 1~4번(초기 세팅, 로그인, 인증 토큰, 플레이리스트 불러오기) 완료. 5번(나이 입력·최대심박수 계산, 운동 종류/플레이리스트 지정)을 진행 중입니다.

## 기술 스택

- **Next.js** — 화면과 API를 한 프로젝트에서 처리. PRD에 정한 대로 고정이며, 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않습니다.
- 데이터 저장: 별도 DB 없이 브라우저 **localStorage**만 사용 (나이, 운동별 플레이리스트 지정, 저장한 곡 기록)
- 배포: **Vercel**만 사용합니다.

## 이번 범위에서 반드시 지킬 것

PRD.md 5번의 규칙은 기능 구현 시 그대로 따라야 합니다. 특히:

- **음악 서비스는 Spotify만.** Apple Music 등 다른 서비스는 연동하지 않습니다.
- **심박수는 가상(mock) 데이터로 처리.** 실제 스마트워치 연동은 하지 않습니다. 사용자의 최대심박수(220-나이) 대비 약 40%~90% 범위에서 5초마다 값이 바뀌는 시뮬레이션으로 구현합니다.
- **앱은 음악을 직접 재생·제어하지 않습니다.** 곡 추천과 곡 변경 안내는 모두 "안내"에 그치고, 실제 재생은 Spotify 앱/웹플레이어에서 사용자가 직접 합니다.
- **추천 후보곡은 직접 준비한 샘플 목록(제목·아티스트·BPM·Spotify 트랙 ID)에서 고릅니다.** 외부 BPM API(GetSongBPM 등)나 Spotify Audio Features API는 쓰지 않습니다. 샘플 곡은 실제 Spotify에 존재하는 곡의 트랙 ID까지 포함해야 검색 없이 바로 저장 기능이 동작합니다.
- **추천은 현재 심박수와 BPM이 가장 가까운 곡 1개**로 단순하게 고릅니다. 구간별 범위 확대나 지속시간 조건 같은 복잡한 로직은 두지 않습니다.
- **Spotify 인증 토큰은 Auth.js 서버 세션(httpOnly 쿠키)에만 보관합니다.** localStorage에 토큰을 두지 않습니다. Spotify API 호출은 Next.js API Route(서버)에서 대신 수행합니다.
- **운동 종류는 고정 목록이 아니라 사용자가 자유 입력**합니다.
- **플레이리스트는 앱 내부에 따로 만들지 않고**, 연결된 Spotify 계정의 실제 플레이리스트만 사용합니다.
- 곡 변경 안내(기능2)는 기능1의 추천 곡이 직전과 달라졌을 때만 함께 동작합니다. 별도의 구간 분류는 없습니다.

세부 숫자(최대심박수 40~90% 범위, 5초 갱신 주기, 샘플 곡 25곡 내외 등)는 PRD.md 5·9번을 그대로 따르세요. 여기서 요약하지 않은 이유는, 숫자가 바뀌면 PRD.md만 고치면 되도록 하기 위함입니다.

## 이번에 만들지 않는 것 (비범위)

PRD.md 6번 참고. 특히 아래는 이 프로젝트에서 다시 언급되더라도 만들지 마세요:

- 소셜 기능, 자체 음악 스트리밍/재생, 운동 기록 통계 대시보드, 스마트워치 자체 앱/실제 연동
- 외부 BPM 데이터베이스 연동, Apple Music 지원
- 심박수 강도 구간(저/중/고강도) 분류, 구간별 플레이리스트 전환 안내
- 곡 추천 시 ±bpm 범위 단계적 확대, 추천 갱신 지속시간 조건

## 디자인

- 미니멀하고 깔끔한 톤. 불필요한 장식 요소 최소화
- 색상: Spotify 브랜드 톤에 맞춘 **블랙 & 그린** 기반 (배경 `#121212`, 텍스트 `#ffffff`, 포인트 `#1db954`). 그린은 주요 액션 버튼·실시간 심박수·상태 안내 등 포인트에만 쓰고, 나머지는 블랙/화이트 톤을 유지한다. 라이트모드 분기 없이 항상 다크 기반 한 가지 테마로 통일한다.
- 모바일 전용, 세로 화면 최적화 (운동 중 손에 들고 보는 상황을 전제)

## 보안

- 비밀 값은 전부 `.env`에만 두고, 코드에 하드코딩하지 않습니다. `.env`와 `node_modules`는 `.gitignore`에 등록해두고, 절대 커밋하지 않습니다.
- 심박수·나이·플레이리스트 지정 등 사용자 데이터는 서버로 전송·저장하지 않고 브라우저 localStorage에만 둡니다.
- 이 문서(PRD.md 포함)는 비공개 내부 문서입니다. 외부에 공개하지 마세요.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용합니다.
  - 예: Supabase가 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업합니다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증합니다.

## 작업 방식

- 모든 설명과 주석은 한국어로 작성합니다.
- 새 파일은 `my-app` 폴더 안에만 만듭니다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려줍니다.
- 파일을 지워야 할 때는 바로 삭제하지 않고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둡니다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제합니다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용합니다.

## 개발 순서

PRD.md 9번 "개발 단위" 목록을 순서대로 따라갑니다. 목록 순서를 임의로 바꾸지 말고, 순서를 바꾸고 싶다면 먼저 사용자에게 확인하세요.
