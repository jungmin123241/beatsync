# DESIGN — 비트싱크 화면·데이터·기술 설계

> 근거 문서: [PLAN.md](PLAN.md) (작업 순서), [PRD.md](PRD.md) (전체 요구사항), [CLAUDE.md](CLAUDE.md) (개발 규칙)

## 1. 화면 구성

총 4개 화면. PRD 4번 이용 흐름 순서대로 이어진다.

```
① 로그인 화면 → ② 온보딩(나이 입력, 최초 1회만) → ③ 운동 설정 화면 → ④ 운동 화면(메인)
```

### ① 로그인 화면 (`/`)

```
┌────────────────────────┐
│                        │
│      비트싱크           │
│   (서비스 이름/로고)      │
│                        │
│  [ Spotify로 시작하기 ]  │
│                        │
└────────────────────────┘
```
- Spotify 로그인 버튼 하나만 있는 화면
- 버튼을 누르면 Spotify 인증 화면으로 이동 → 인증 성공 시 ②(최초) 또는 ③(이후)으로 이동

### ② 온보딩 화면 (`/onboarding`, 최초 로그인 시 1회만)

```
┌────────────────────────┐
│  나이를 알려주세요        │
│                        │
│   [ 숫자 입력창 ]         │
│                        │
│      [ 다음 ]           │
└────────────────────────┘
```
- 나이 입력창 + "다음" 버튼
- 입력한 나이로 최대심박수(220-나이)를 계산해 localStorage에 저장 → ③으로 이동

### ③ 운동 설정 화면 (`/setup`)

```
┌────────────────────────┐
│  운동 이름을 입력하세요     │
│  [ 텍스트 입력창 ]         │
│                        │
│  이 운동에 쓸 플레이리스트   │
│  [ 내 Spotify 플레이리스트  │
│    목록에서 선택 ]         │
│                        │
│      [ 운동 시작 ]        │
└────────────────────────┘
```
- 운동 이름(자유 입력) + 이번 운동에서 쓸 Spotify 플레이리스트 선택
- 플레이리스트 목록에는 **본인이 만들었거나 공동 작업으로 지정된 것만** 나온다
  (팔로우만 한 플레이리스트는 곡 추가가 막혀 있어 제외 — 5번 API 명세 참고)
- 이미 등록해둔 운동 이름이 있으면 목록으로 보여주고 재사용 가능
- "운동 시작"을 누르면 ④로 이동, 선택 내용은 localStorage에 저장
- 화면 하단에 "나이 다시 입력" 링크 하나 배치 (나이를 잘못 입력했을 때 복구용, 누르면 ②로 이동)

### ④ 운동 화면 (`/workout`) — 메인 화면

```
┌────────────────────────┐
│   ❤ 132 bpm            │  ← 실시간(가상) 심박수 (5초마다 갱신)
├────────────────────────┤
│  ♪ 곡이 바뀌었어요        │  ← 추천 곡이 바뀐 직후에만 표시
├────────────────────────┤
│  🎵 추천 곡               │
│  곡 제목 - 아티스트 (128bpm)│
│                        │
│      [ 저장 ]           │  ← 지정 플레이리스트에 곡 추가
├────────────────────────┤
│      [ 운동 종료 ]        │
└────────────────────────┘
```
- 상단: 가상 심박수 실시간 숫자만 표시 (강도 구간 뱃지 없음 — PoC 단순화로 삭제)
- 심박수 변화로 추천 곡이 이전과 달라진 직후에만 "곡이 바뀌었어요" 안내 표시 (기능2)
- 중앙: 현재 추천 곡 1개(심박수와 BPM이 가장 가까운 곡) + "저장" 버튼 (기능1)
- 하단: "운동 종료" → ③ 운동 설정 화면으로 복귀
- 화면 진입 즉시, 그 순간의 심박수를 기준으로 첫 곡을 바로 추천한다 (빈 화면으로 시작하지 않음)

### 라우팅 규칙

- 로그인 안 된 상태로 `/onboarding`, `/setup`, `/workout`에 접근하면 → `①`(`/`)로 이동
- 로그인은 됐지만 나이(최대심박수) 정보가 없는 상태로 `/setup`, `/workout`에 접근하면 → `②`(`/onboarding`)로 이동

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[입력]                [처리]                        [출력]

Spotify 로그인    →   OAuth 인증 토큰 발급        →   Auth.js 서버 세션(httpOnly
                     (Auth.js가 처리)               쿠키)에 토큰 보관. 이후 Spotify
                                                     API 호출은 전부 Next.js API
                                                     Route(서버)가 대신 수행하고,
                                                     브라우저(localStorage)에는
                                                     토큰을 두지 않음

나이 입력         →   최대심박수 = 220 - 나이     →   localStorage에 저장,
(최초 1회)                                          이후 화면에서 재사용

운동 이름 +       →   localStorage에 저장          →   운동 화면 진입 시
플레이리스트 선택                                    "현재 운동"으로 불러옴

가상 심박수        →   5초마다 새 값 생성           →   화면에 심박수 숫자 갱신
발생기 (5초 간격)     (최대심박수의 40~90% 범위)

                  →   샘플 곡 목록(트랙 ID 포함)   →   추천 곡 카드 갱신.
                     에서 BPM이 가장 가까운           직전 추천 곡과 달라졌으면
                     곡 1개 선택                      "곡이 바뀌었어요" 안내 표시

"저장" 버튼 클릭    →   샘플 곡의 트랙 ID로 검색     →   저장 완료 표시,
                     없이 바로 Spotify API 호출로     (중복 곡이면 건너뜀)
                     지정 플레이리스트에 추가
                     (이미 있는 곡이면 건너뜀)
```

핵심 원칙: 5초마다 갱신되는 가상 심박수를 기준으로 가장 가까운 BPM의 곡을 다시 고르고(기능1), 그 결과가 직전과 다르면 화면에 안내만 띄운다(기능2) — 별도의 구간 분류나 지속시간 조건 없이, 하나의 흐름으로 이어지는 단순한 로직이다.

### 샘플 곡 목록의 BPM 배치 규칙

추천 로직이 "가장 가까운 BPM 1곡"이라 단순한 만큼, **곡 목록의 BPM 분포가 곧 추천 다양성을 결정한다.** `src/lib/sampleSongs.ts`에서 두 가지를 지킨다.

- **BPM을 중복시키지 않는다.** 같은 BPM이 둘이면 앞쪽 곡만 계속 뽑히고 뒤쪽 곡은 영영 추천되지 않는다. 겹치면 ±1 조정한다.
- **BPM 사이를 고르게 벌린다.** 값 사이가 크게 비면 그 근처 곡 하나가 넓은 심박수 구간을 독차지해 같은 곡만 반복된다. 오름차순으로 정렬해 두어 빈 구간이 눈에 띄게 한다.

## 3. 기술 선택

기본은 **Next.js**로 고정 (PRD·CLAUDE.md에 이미 정해둔 대로). 오늘 안에 Vercel 배포까지 마치는 걸 목표로, 꼭 필요한 것만 최소로 추가한다.

| 기술 | 왜 필요한가 (쉬운 설명) |
|---|---|
| **Next.js** (기본) | 화면과 서버 코드를 한 프로젝트에서 만들고, Vercel에 바로 배포할 수 있는 도구 |
| **Auth.js (next-auth)** | Spotify 로그인 버튼을 누르면 인증하고 토큰을 받아오는 과정을, 직접 처음부터 짜지 않고 검증된 방식으로 빠르고 안전하게 처리해주는 도구. 토큰은 브라우저가 아니라 서버 쿠키 세션에 보관되어 더 안전함 |
| **Tailwind CSS** | 미니멀·블랙&화이트 디자인을, CSS 파일을 따로 짜지 않고 클래스 이름만으로 빠르게 입히는 도구 (Next.js 프로젝트 생성 시 기본 옵션으로 바로 켤 수 있음) |

그 외 추가 설치는 없다:
- **상태 관리 라이브러리(Zustand 등) 불필요** — 화면이 4개뿐이고 공유할 값(심박수, 추천 곡, 로그인 정보)도 몇 개 안 돼서, React 기본 기능(useState, Context)만으로 충분
- **DB 불필요** — PRD 결정대로 모든 데이터는 브라우저 localStorage에만 저장
- **별도 백엔드 서버 불필요** — Next.js API Routes로 Spotify와 주고받는 요청만 처리

## 4. localStorage 데이터 모델

화면 간 데이터를 주고받는 유일한 통로이므로, 키와 형태를 아래처럼 고정한다.

| 키 | 형태 | 용도 |
|---|---|---|
| `beatsync.profile` | `{ age: number, maxHr: number }` | 나이·최대심박수 (②에서 저장) |
| `beatsync.workouts` | `[{ name: string, playlistId: string, playlistName: string }]` | 지금까지 등록한 운동 종류 목록 (③에서 추가) |
| `beatsync.currentWorkout` | `{ name: string }` | 지금 진행 중인 운동 이름 (③→④ 전달용) |
| `beatsync.savedTracks` | `{ [playlistId: string]: string[] }` | 플레이리스트별로 저장(추가)한 트랙 ID 기록 — 중복 저장 방지에 사용 |

## 5. API 명세 (Spotify 연동)

Spotify와 주고받는 요청은 전부 브라우저가 아니라 **Next.js API Route(서버)** 가 대신 처리한다 (토큰이 서버 세션에만 있으므로).

**로그인 시 요청할 Spotify 권한 범위(scope)**: `playlist-read-private playlist-modify-public playlist-modify-private`
(이 3개가 없으면 내 비공개 플레이리스트 조회·곡 추가가 막힌다)

| Method | 경로 | 설명 |
|---|---|---|
| GET | `/api/playlists` | 로그인한 사용자의 Spotify 플레이리스트 목록 조회 (③ 화면에서 사용) |
| POST | `/api/playlists/[id]/tracks` | 지정 플레이리스트에 트랙 추가 (④ "저장" 버튼) — 요청 본문에 `spotifyTrackId` 포함 |

중복 저장 방지는 API 조회 없이 `beatsync.savedTracks`(localStorage) 기록만으로 판단한다.
그래서 트랙 목록 조회용 GET은 두지 않는다.

**플레이리스트 목록은 "저장 가능한 것"만 내려준다.** Spotify는 본인 소유이거나
공동 작업(collaborative)으로 설정된 플레이리스트에만 곡 추가를 허용하고, 그 외에는
저장 시 `403`을 돌려준다. 그래서 `/v1/me`로 내 계정 ID를 확인해 소유자가 나이거나
collaborative인 항목만 걸러서 ③ 화면에 보여준다 (`src/lib/spotify.ts`).
팔로우만 한 플레이리스트는 애초에 선택지에 나오지 않는다.

## 6. 에러 처리

| 상황 | 처리 |
|---|---|
| Spotify 인증 실패/거부 | 로그인 화면(①)에 "로그인에 실패했어요, 다시 시도해주세요" 메시지 표시 |
| 액세스 토큰 만료 (운동 중 1시간 경과 등) | `src/auth.ts`의 `jwt` 콜백이 리프레시 토큰으로 자동 재발급 → 실패하면 세션에서 토큰을 비워 API가 401을 내고, ①로 이동시켜 재로그인 요청 |
| 플레이리스트 목록/트랙 저장 API 실패 | 화면 하단에 동일한 형태의 토스트 메시지로 통일: "문제가 발생했어요, 다시 시도해주세요" |
| 트랙 저장 시 Spotify가 `403` 반환 | 서버 로그에 Spotify 응답 상태·본문을 남긴다. 원인은 대개 둘 중 하나 — ⓐ 곡 추가 권한이 없는 플레이리스트(→ 목록 필터로 예방, 5번 참고) ⓑ 앱 소유자 계정의 Premium 상태 미반영(코드로 해결 불가, Spotify 쪽 반영 대기) |

## 7. 배포 준비 (환경변수 · Spotify 설정)

| 환경변수 | 용도 |
|---|---|
| `SPOTIFY_CLIENT_ID` | Spotify 앱 클라이언트 ID |
| `SPOTIFY_CLIENT_SECRET` | Spotify 앱 클라이언트 시크릿 |
| `AUTH_SECRET` | Auth.js 세션 암호화 키 |
| `NEXTAUTH_URL` | 배포된 앱 주소 (로컬 개발 중엔 `http://127.0.0.1:3000` — Spotify가 `localhost`라는 이름은 HTTP로 허용하지 않고 `127.0.0.1`만 허용함) |

Spotify 개발자 대시보드의 앱 설정에 **Redirect URI를 2개** 등록해야 한다:
1. `http://127.0.0.1:3000/api/auth/callback/spotify` (로컬 개발용 — `localhost`가 아니라 `127.0.0.1`로 등록해야 Spotify가 허용함)
2. `https://beatsync-lemon.vercel.app/api/auth/callback/spotify` (배포용)

### 배포 현황

| 항목 | 값 |
|---|---|
| 프로덕션 URL | https://beatsync-lemon.vercel.app |
| GitHub 저장소 | https://github.com/jungmin123241/beatsync |
| 배포 방식 | Vercel CLI로 `main` 브랜치 내용을 프로덕션에 직접 배포 |

`NEXTAUTH_URL`을 포함한 위 4개 환경변수는 Vercel 프로젝트의 Production 환경에 등록되어 있다.

### 로컬 개발 시 주의 (localhost / 127.0.0.1 함정)

Spotify가 `localhost`라는 이름을 허용하지 않아, 로컬 개발 중에는 반드시 **`http://127.0.0.1:3000`** 으로 접속해야 한다. 그런데 Next.js 16 개발 서버는 요청 주소를 항상 `localhost`로 보고하고, `NextRequest`로는 이 주소를 바꿀 수 없어 next-auth의 자체 보정(`NEXTAUTH_URL`)이 동작하지 않는다. 그래서 두 가지 장치를 두었다:

- `src/app/api/auth/[...nextauth]/route.ts` — next-auth 기본 핸들러 대신 Auth.js를 직접 호출하면서, 표준 `Request`로 주소를 `127.0.0.1`로 맞춰 넘긴다
- `src/app/layout.tsx` — 브라우저가 `localhost`로 들어오면 `127.0.0.1`로 옮긴다 (로그인 쿠키가 두 주소로 쪼개지는 것을 방지)
