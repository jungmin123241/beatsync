# PLAN — 비트싱크 1차 개발 사이클

> 근거 문서: [PRD.md](PRD.md) (전체 요구사항), [CLAUDE.md](CLAUDE.md) (개발 규칙)

## 이번 사이클 목표

PRD.md 6번 범위에 정의된 세 가지 기능(계정 연동, 심박수 기반 곡 추천·저장, 구간별 전환 안내)을 Next.js로 구현해, 가상 심박수로 전체 흐름이 처음부터 끝까지 동작하는 상태로 Vercel에 배포한다.

## 성공 기준

- [ ] PRD 5번의 세 기능(0. 계정 연동, 1. 곡 추천·저장, 2. 구간 전환 안내)이 모두 동작한다
- [ ] Spotify 로그인 → 나이 입력 → 운동 종류 입력·플레이리스트 지정 → 가상 심박수로 곡 추천 → 저장 → 곡 변경 안내까지, PRD 4번 이용 흐름이 끊김 없이 이어진다
- [ ] 추천 곡이 현재 심박수와 유사한 BPM으로 선택된다.
- [ ] "저장" 버튼으로  Spotify 플레이리스트에 곡이 추가된다 
- [ ] Apple Music 등은 구현하지 않는다.
- [ ] Vercel에 배포되어 실제 URL로 접속·시연 가능하다

## 작업 순서

먼저 되면 나머지가 쉬워지는 순서로 정렬. (PRD 9번 개발 단위와 동일한 순서)

1. Next.js 프로젝트 초기 세팅 및 기본 화면 뼈대 만들기
2. Spotify 계정 연동 로그인 화면 만들기
3. 음악 서비스 인증 토큰 발급·저장 처리 (API 키는 `.env`로 관리)
4. 로그인한 사용자의 플레이리스트 목록 불러오기
5. 최초 로그인 직후 나이 입력 온보딩 화면 만들고 최대심박수(220 - 나이) 계산 로직 구현 (localStorage에 저장, 최초 1회만), 운동 종류 입력, 운동별 Spotify 플레이리스트 선택
6. 가상 심박수(mock) 발생기 만들기 — 최대심박수 (220- 나이)의 약 40-90% 범위로 5초마다 값을 변경하고 화면에 실시간으로 표시해줘.
7. 샘플 곡 목록(제목·아티스트·BPM·Spotify 트랙 ID) 데이터 파일 만들기 — 실제 Spotify에 존재하는 곡으로 25곡 내외, 트랙 ID까지 포함 (BPM 중복 없이 고르게 분포)
8. 가장 가까운 BPM 한 곡 선택 - 추천곡 표시, Spotify 플레이리스트 저장 버튼 구현
9. 심박수가 바뀌면 즉시 추천 - 곡 변경 시 안내 메세지 표시
10. Vercel 배포 및 환경변수 설정
